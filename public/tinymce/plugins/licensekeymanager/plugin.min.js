(function(){
  if (typeof tinymce === 'undefined') return;
  /*
    Minimal no-op licensekeymanager plugin to avoid 404s when TinyMCE
    attempts to load the commercial license manager. This prevents the
    editor from failing to load because the plugin file is missing.
    NOTE: This is a harmless shim and does not provide any license
    validation. If you have a commercial TinyMCE license, please use
    the official plugin or provide a valid API/license key.
  */
  tinymce.PluginManager.add('licensekeymanager', function(editor) {
    return {
      /* no-op plugin: keep TinyMCE happy */
    };
  });
})();
